server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location /v2/ {
    proxy_pass https://api.voischat.cn;
    proxy_ssl_server_name on;
    proxy_set_header Host api.voischat.cn;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
